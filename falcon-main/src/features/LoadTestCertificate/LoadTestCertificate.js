import * as React from 'react';
import ListCranes from '../../components/ListCranes';

const LoadTestCertificate = ({route}) => {
  return <ListCranes page="LoadTest"/>;
};

export default LoadTestCertificate;
