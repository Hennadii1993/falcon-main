import * as React from 'react';
import ListCranes from '../../components/ListCranes';

const AntiCollisionSignOffSheet = ({route}) => {
  return <ListCranes page="AntiCollision"/>;
};

export default AntiCollisionSignOffSheet;
