import * as React from 'react';
import ListCranes from '../../components/ListCranes';

const WorkInstructionsCreateNew = ({route}) => {
  return <ListCranes page="WorkInstructions"/>;
};

export default WorkInstructionsCreateNew;
