import * as React from 'react';
import ListCranes from '../../components/ListCranes';

const SlewRingBoltSheet = ({route}) => {
  return <ListCranes page="SlewRingBoltSheet"/>;
};

export default SlewRingBoltSheet;
