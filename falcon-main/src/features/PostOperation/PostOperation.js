import * as React from 'react';
import ListCranes from '../../components/ListCranes';

const PostOperation = ({route}) => {
  return <ListCranes page="PostOperation"/>;
};

export default PostOperation;
